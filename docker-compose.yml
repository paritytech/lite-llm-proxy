# Copyright (C) Parity Technologies (UK) Ltd.
# SPDX-License-Identifier: Apache-2.0

name: team-llm

services:
  caddy:
    image: caddy:2.8
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - litellm
    networks: [internal]

  litellm:
    image: ghcr.io/berriai/litellm-database:v1.95.0   # PINNED — latest stable confirmed 2026-08-04
    restart: unless-stopped
    env_file: [.env]
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    command: ["--config", "/app/config.yaml", "--port", "4000"]
    expose:
      - "4000"
    depends_on:
      postgres:
        condition: service_healthy
    networks: [internal]

  # ---- PII scrub pair (used only by scripts/export-logs.sh) ----
  # Behind the `scrub` compose profile so a plain `docker compose up -d` does
  # NOT start them: the analyzer's NLP model holds ~1.5 GB RAM, so the nightly
  # export brings them up for the few minutes it runs and stops them after.
  # Ports bind to localhost only — the scrub script talks to them from the
  # host; nothing else should be able to.
  presidio-analyzer:
    image: mcr.microsoft.com/presidio-analyzer:2.2.364   # PINNED — latest confirmed 2026-07-27
    profiles: [scrub]
    restart: "no"
    ports:
      - "127.0.0.1:5002:3000"
    networks: [internal]

  presidio-anonymizer:
    image: mcr.microsoft.com/presidio-anonymizer:2.2.364   # PINNED — keep in lockstep with the analyzer
    profiles: [scrub]
    restart: "no"
    ports:
      - "127.0.0.1:5001:3000"
    networks: [internal]

  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: litellm
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: litellm
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U litellm -d litellm"]
      interval: 5s
      timeout: 5s
      retries: 12
    networks: [internal]

volumes:
  caddy_data:
  caddy_config:
  postgres_data:

networks:
  internal:
    driver: bridge
